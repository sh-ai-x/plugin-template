# Git workflow

- Keep each change on a focused branch or worktree.
- Do not assume a fixed worktree path; use the current working directory.
- Keep generated plugin files self-contained and review the manifest diff before publishing.
- Run the repository's validation and smoke checks before opening a pull request.
- Never put credentials or machine-local absolute paths in a plugin package.
