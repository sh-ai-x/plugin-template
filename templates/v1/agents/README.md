# Agents

This optional directory is for host-neutral agent definitions and delegation notes. Keep an agent's
scope narrow, state its allowed tools and expected output, and link back to the shared rules. Do not
put host-specific startup instructions here; use the adapter manifests and pointer documents.
