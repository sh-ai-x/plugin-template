#!/usr/bin/env bash
set -euo pipefail

# Replace this no-op with a deterministic audit or formatter. Keep stdout empty unless
# the host-specific hook contract explicitly expects structured output.
cat >/dev/null || true
exit 0
