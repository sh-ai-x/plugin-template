# Workflow scenarios

Use this directory for longer scenario narratives and acceptance checklists. Link each scenario to
the skill, hook, rule, or MCP server that implements it. Keep the short execution contract in
`workflows/` and the detailed context here.
