# Scopes

Document whether a setting belongs to the plugin package, the consuming repository, the user profile,
or the current session. Plugin packages should not assume a user-level path or silently modify a
consumer's global configuration.
