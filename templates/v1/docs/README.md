# Documentation

Start with `CODEBASE-MAP.md`, then read the root pointer for the host in use. Put architecture and
workflow explanations here; keep executable behavior in skills, hooks, MCP configuration, and scripts.
